#include <stdio.h>
#include <stdlib.h>
#define pi 3.14159

int main(int argc, char *argv[]) {

	float raio, area, raioQuadrado;
	
	printf ("insira o valor de R: \n");
	scanf ("%f" , &raio);
	
	raioQuadrado = raio * raio;
	area = pi * raioQuadrado;
	
	printf ("A Area do circulo de raio %f = %f" , raio, area);
	 
	 
	int B,b,h,areaT;
	 
	printf ("Area do Trapezio de lado B : %f; b:%f h:%f = %f", scanf("%f", &B), scanf("%f", &b),scanf("%f",&h))
 
	return 0;
}
